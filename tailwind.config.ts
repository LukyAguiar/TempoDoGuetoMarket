// Tailwind v4 não usa este arquivo para tema — tudo vai no globals.css via @theme
// Mantido apenas para compatibilidade de IDEs
import type { Config } from "tailwindcss";
const config: Config = { content: [] };
export default config;
